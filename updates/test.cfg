id = testUpdate;
name = testUpdate.name;
info = testUpdate.info;

language en {
	name = Test update;
	info = [some info];
};

language it {
	name = Aggiornamento test;
	info = [qualche info];
};

script = message testUpdate.info;