achievement beginner {
	name = Beginner;
	info = Complete first level;
	
	verify = ( $completedLevels > 1 ) | ( $completedLevels = 1 );
	
	image icon {
		imageFile = data/graphics/misc/achievements.png;
		rectangle rect { x = 0; y = 0; w = 48; h = 48; };
	};
};